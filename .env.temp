# -----------------------------------------------------------------------------
# NEXT.JS GENERAL CONFIGURATION (Optional, but good to know)
# -----------------------------------------------------------------------------
# If you need to set a custom port for development
# PORT=3000

# -----------------------------------------------------------------------------
# SUPABASE CONFIGURATION
# Required for connecting to your Supabase project (Auth, Database, Storage)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL="https://YOUR_SUPABASE_PROJECT_REF.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_PUBLIC_KEY"

# If you need the service role key for specific server-side operations (e.g., bypassing RLS,
# managing users directly). DANGER: NEVER EXPOSE THIS ON THE CLIENT-SIDE!
# SUPABASE_SERVICE_ROLE_KEY="YOUR_SUPABASE_SERVICE_ROLE_KEY"


# -----------------------------------------------------------------------------
# STRIPE CONFIGURATION
# Required for processing payments
# -----------------------------------------------------------------------------
# Your Stripe Publishable Key (safe to expose to the client)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_YOUR_STRIPE_PUBLISHABLE_KEY"

# Your Stripe Secret Key (DANGER: ONLY USE ON THE SERVER-SIDE!)
STRIPE_SECRET_KEY="sk_test_YOUR_STRIPE_SECRET_KEY"

# Your Stripe Webhook Signing Secret (DANGER: ONLY USE ON THE SERVER-SIDE!)
# Essential for verifying webhook events from Stripe for security.
STRIPE_WEBHOOK_SECRET="whsec_YOUR_STRIPE_WEBHOOK_SECRET"
